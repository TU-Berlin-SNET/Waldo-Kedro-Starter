"""
Anomaly detection nodes
"""

def anomaly_detection(data: pd.DataFrame, params: dict) -> pd.DataFrame:
    """
        Calculate outlier score for the input dataframe, on the list of columns specified inside ``params.cols``.

        :param data: input dataframe
        :param params: parameters for the anomaly detection model

        :return: a dataframes with anomaly detection scores and predictions
    """

    # Do your own anomaly-detection implementation here
    print("TODO: anomaly-detection")

    return data
